# Copy to .env (gitignored), then: npm run deploy:gem
GEM_USER=root
GEM_HOST=gem.bestalbumsintheuniverse.com   # reachable via ~/.ssh/config (passwordless)
# GEM_PATH=/srv/gemini/content/gem.bestalbumsintheuniverse.com  # optional; this is the default
