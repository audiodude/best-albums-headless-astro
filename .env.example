# Copy to .env (gitignored), then: npm run deploy:gem
GEM_USER=your-ssh-user
GEM_HOST=your.gemini.host        # reachable via ~/.ssh/config (passwordless)
# GEM_PATH=/var/gem/best-albums  # optional; defaults to the legacy capsule dir
